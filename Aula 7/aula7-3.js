// Operadores lógicos

// AND (E) &&
// OR (OU) ||
// NOT (NÃO) !

// se todos os valores forem verdade ele retorna
console.log(true && true)

// quando pelo menos um deles é verdade ele retorna
console.log(true || false);

// valor contrario
console.log(!true );